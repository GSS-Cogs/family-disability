pipeline {
    agent {
        label 'master'
    }
    stages {
        stage('Transform') {
            agent {
                docker {
                    image 'cloudfluff/databaker'
                    reuseNode true
                }
            }
            steps {
                     !sh jupyter-nbconvert --to python 'DWP-Family-Resources-Survey-Carer/sic2007.ipynb' --stdout
            }
        }
  
    }
    
}

